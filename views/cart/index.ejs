```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>Cart | Zoi</title>

  <link rel="stylesheet" href="/css/style.css">
</head>

<body>

  <%- include('../partials/_navCustomer.ejs') %>


  <!-- Cart -->

  <main class="cart-page">

    <h1>Your Cart</h1>


    <% if (cart.length === 0) { %>

      <section class="empty-cart">

        <h2>Your cart is empty</h2>

        <p>
          You haven't added any products yet.
        </p>

        <a href="/shop" class="continue-shopping">
          Continue Shopping
        </a>

      </section>


    <% } else { %>


      <div class="cart-layout">


        <!-- Cart Products -->

        <section class="cart-products">

          <% cart.forEach(item => { %>

            <div class="cart-card">


              <!-- Product Image -->

              <div class="cart-product-image">


                <!-- Haircare -->

                <% if (item.category === 'haircare' && item.name === 'product one') { %>

                  <img
                    src="/imges-acss/hair2.png"
                    alt="<%= item.name %>"
                  >

                <% } else if (item.category === 'haircare' && item.name === 'product two') { %>

                  <img
                    src="/imges-acss/hair1.png"
                    alt="<%= item.name %>"
                  >


                <!-- Tools & Accessories -->

                <% } else if (item.category === 'tools&accessories' && item.name === 'product three') { %>

                  <img
                    src="/imges-acss/product1.png"
                    alt="<%= item.name %>"
                  >

                <% } else if (item.category === 'tools&accessories' && item.name === 'second product') { %>

                  <img
                    src="/imges-acss/product3.png"
                    alt="<%= item.name %>"
                  >

                <% } else if (item.category === 'tools&accessories' && item.name === 'product one') { %>

                  <img
                    src="/imges-acss/product2.png"
                    alt="<%= item.name %>"
                  >


                <!-- Makeup -->

                <% } else if (item.category === 'makeup' && item.name === 'first') { %>

                  <img
                    src="/imges-acss/makeup1.png"
                    alt="<%= item.name %>"
                  >

                <% } else if (item.category === 'makeup' && item.name === 'second') { %>

                  <img
                    src="/imges-acss/makeup2.png"
                    alt="<%= item.name %>"
                  >

                <% } else if (item.category === 'makeup' && item.name === 'third') { %>

                  <img
                    src="/imges-acss/makeup3.png"
                    alt="<%= item.name %>"
                  >

                <% } else if (item.category === 'makeup' && item.name === 'fourth') { %>

                  <img
                    src="/imges-acss/makeup4.png"
                    alt="<%= item.name %>"
                  >


                <!-- Skincare -->

                <% } else if (item.category === 'skincare' && item.name === 'fifth the eye patches') { %>

                  <img
                    src="/imges-acss/skin.png"
                    alt="<%= item.name %>"
                  >

                <% } %>


              </div>


              <div class="cart-product-info">

                <p class="product-category">
                  <%= item.category %>
                </p>

                <h2>
                  <%= item.name %>
                </h2>

                <p class="cart-price">
                  <%= item.price %> BHD
                </p>


                <!-- Update Quantity -->

                <form
                  action="/cart/<%= item.product %>?_method=PUT"
                  method="POST"
                  class="quantity-form"
                >

                  <label for="quantity-<%= item.product %>">
                    Quantity
                  </label>

                  <div class="quantity-controls">

                    <input
                      type="number"
                      id="quantity-<%= item.product %>"
                      name="quantity"
                      value="<%= item.quantity %>"
                      min="1"
                      required
                    >

                    <button type="submit">
                      Update
                    </button>

                  </div>

                </form>


                <!-- Remove -->

                <form
                  action="/cart/<%= item.product %>?_method=DELETE"
                  method="POST"
                >

                  <button
                    type="submit"
                    class="remove-button"
                  >
                    Remove
                  </button>

                </form>

              </div>

            </div>

          <% }) %>

        </section>


        <!-- Cart Summary -->

        <aside class="cart-summary">

          <h2>Order Summary</h2>

          <div class="summary-line">

            <span>Items</span>

            <span>
              <%= cart.reduce((total, item) => total + Number(item.quantity), 0) %>
            </span>

          </div>


          <div class="summary-line">

            <span>Total</span>

            <strong>
              <%= cart.reduce((total, item) => total + (item.price * item.quantity), 0).toFixed(2) %>
              BHD
            </strong>

          </div>


          <a
            href="/checkout"
            class="checkout-button"
          >
            Checkout
          </a>


          <a
            href="/shop"
            class="continue-shopping cart-continue"
          >
            Continue Shopping
          </a>

        </aside>

      </div>

    <% } %>

  </main>

</body>
</html>
```
